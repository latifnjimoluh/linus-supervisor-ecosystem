'use client';

export default function TemplateDetailPage({ params }) {
  return (
    <div>
      <h1 className="text-2xl font-bold">Template Detail</h1>
      {/* TODO: show template {params.id} */}
    </div>
  );
}
