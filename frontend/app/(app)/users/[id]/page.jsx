'use client';

export default function UserDetailPage({ params }) {
  return (
    <div>
      <h1 className="text-2xl font-bold">User Detail</h1>
      {/* TODO: load details for {params.id} */}
    </div>
  );
}
