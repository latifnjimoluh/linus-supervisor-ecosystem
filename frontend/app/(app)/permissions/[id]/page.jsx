'use client';

export default function PermissionDetailPage({ params }) {
  return (
    <div>
      <h1 className="text-2xl font-bold">Permission Detail</h1>
      {/* TODO: load permission {params.id} */}
    </div>
  );
}
